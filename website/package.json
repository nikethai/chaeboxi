{
  "name": "chaeboxi-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "pnpm exec astro build",
    "preview": "astro preview",
    "test": "node --experimental-strip-types --test src/lib/latest-release.test.ts"
  },
  "devDependencies": {
    "astro": "^5.13.0"
  }
}
