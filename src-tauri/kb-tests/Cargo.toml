[package]
name = "chaeboxi-kb-tests"
version = "0.1.0"
edition = "2021"
publish = false
description = "Standalone unit tests for src-tauri/src/kb logic (no GTK/Tauri/reqwest)"
rust-version = "1.85"

[dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
