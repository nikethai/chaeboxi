import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.chaeboxi.app',
  appName: 'Chaeboxi',
  webDir: 'release/app/dist/renderer',
}

export default config
